# React 修改配置文件

## npm run reject（笨重）

根目录运行该命令，create-react-app 创建的项目的配置项暴露出来，不可逆，笨重

## react-app-rewired（不维护了）

安装 react-app-rewired 插件，但是不推荐，因为这个库不维护了。

## craco

推荐安装 [craco](https://craco.js.org/)

例子 1，让 react 项目支持装饰器

安装
